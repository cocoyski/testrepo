---

- hosts: localhost
  connection: local
  become: true

  pre_tasks: 
  - name: Update repositories
    apt:
      update_cache: yes
    changed_when: false

  - name: Install Packages
    apt:
      name: 
         - apache2
         - cowsay
    
